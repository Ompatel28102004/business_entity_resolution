# Pinned to the versions verified during development (Python 3.12).
# CPU-only pipeline -- no GPU / CUDA dependencies anywhere.
numpy==2.5.3
pandas==3.0.6
scipy==1.18.1
scikit-learn==1.9.1
rapidfuzz==3.14.6
pyarrow==25.0.1
joblib==1.6.0
psutil==6.0.0
pyyaml>=6.0
